/**
 * STUB — remote viewing / separate partition
 *
 * Intentionally not implemented.
 *
 * Hosting path is local-first (`npm start`). GitHub Pages can serve the
 * built client; OnRender (or similar) can host this Express API later.
 *
 * Do not add: RDP, VNC, screen share, or desktop streaming here.
 *
 * If/when remote viewing is added, it should live on a separate
 * process/partition with auth — not on the public resume API.
 */
export const remoteViewing = {
  enabled: false,
};
